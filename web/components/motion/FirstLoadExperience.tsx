"use client";

/**
 * FirstLoadExperience — re-exported as an alias for MaitriIntro.
 *
 * The old multi-phase product tour has been replaced by the cinematic
 * 2.5-second brand intro (MaitriIntro). This file is kept so nothing
 * in the rest of the codebase (layout, tests, etc.) needs to rename imports.
 */

export { MaitriIntro as FirstLoadExperience } from "@/components/motion/MaitriIntro";
